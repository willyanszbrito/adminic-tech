version: "3.8"

services:
  # 1. Backend API (FastAPI + Uvicorn)
  adminic-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    environment:
      - PORT=8000
      - ENVIRONMENT=production
      - APP_DOMAIN=ia.adminic.com.br
      - API_DOMAIN=api.adminic.com.br
      - CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000","https://ia.adminic.com.br","https://campelo.adminic.com.br","https://segredos.adminic.com.br","https://segredosdocorte.adminic.com.br","*"]
      - DATABASE_URL=postgresql://adminic_user:${DB_PASSWORD:-adminic_secure_pass_2026}@postgres:5432/adminic_db
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-adminic_jwt_production_master_secret_2026}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-564423794141-eum30ivtuprbv8ikk8qi6b7gapgm6a2v.apps.googleusercontent.com}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - MERCADO_PAGO_PUBLIC_KEY=${MERCADO_PAGO_PUBLIC_KEY}
      - MERCADO_PAGO_ACCESS_TOKEN=${MERCADO_PAGO_ACCESS_TOKEN}
      - META_ACCESS_TOKEN=${META_ACCESS_TOKEN}
      - META_PHONE_ID=${META_PHONE_ID}
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
    volumes:
      - adminic-data:/app/data
    depends_on:
      - postgres
      - redis
    expose:
      - "8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - adminic-network

  # 2. Database (PostgreSQL 16 Enterprise)
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_DB: adminic_db
      POSTGRES_USER: adminic_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-adminic_secure_pass_2026}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    expose:
      - "5432"
    networks:
      - adminic-network

  # 3. Cache & Distributed Rate Limiting (Redis 7)
  redis:
    image: redis:7-alpine
    restart: always
    volumes:
      - redis-data:/data
    expose:
      - "6379"
    networks:
      - adminic-network

volumes:
  adminic-data:
  postgres-data:
  redis-data:

networks:
  adminic-network:
    driver: bridge
