# Frontend Zero-Config Architecture
# O frontend do Adminic Smart Booking não utiliza variáveis de ambiente em tempo de build (VITE_* ou NEXT_PUBLIC_*).
# Todas as configurações públicas e de integração são fornecidas dinamicamente e de forma segura pelo backend via /api/v1/config/public.
